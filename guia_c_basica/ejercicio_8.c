#include <stdio.h>

int main() {
	int i = 5;
	int j = 10;
	printf("El operador preincremento ++i, primero incrementa i y luego devuelve el valor de i:\n"
		"Si tenemos que i vale %d", i);
	printf(" vamos a tener que ++i vale %d", ++i);
	printf(" e i vale %d\n", i);
	


}